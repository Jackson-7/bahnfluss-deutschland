[project]
name = "bahnfluss-deutschland"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jackson-7", email = "65776593+Jackson-7@users.noreply.github.com" }
]
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.10.0",
    "pandas>=2.2.0",
    "tqdm>=4.66.0",
]

[project.scripts]
bahnfluss-deutschland = "bahnfluss_deutschland.cli:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
]
